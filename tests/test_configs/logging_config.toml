[[tasks]]
type = "Custom"
name = "LoggedTask"

dependencies = []
time_multiplier = 1.0

[logging]
log_file = "simulation_log.csv"
log_interval_millis = 100
logged_outputs = { LoggedTask = ["custom_output"] }
