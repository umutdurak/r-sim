# Task-Based Simulation Architecture

In r-sim, every simulation is composed of **tasks** — self-contained units of computation that execute once per time step. This chapter explores how tasks are defined, implemented, and combined into a simulation.
