# Web Interface and Runtime Control

r-sim includes a built-in web server (powered by [Warp](https://docs.rs/warp/)) that provides real-time monitoring, parameter tuning, and lifecycle control through HTTP endpoints.
